// So this wrapper is because FACC doesn't currently support function pointers or void* pointers.
// The aim is to provide a roughly representative approach
// that allows us to benchmark w/out the engineering work
// required.
